name = "browser-swarm"
description = "Interactive headless-browser agent for form-driven flows and sites that APIs or web search cannot handle. Spawn this custom agent type with fork_turns=\"none\". Multiple agents of this type can run concurrently; each spawned agent owns a separate MCP session and isolated context. After browser_close or five idle minutes, spawn a fresh agent for further browser work. Resuming a closed agent does not create a new context. In-flight requests suspend the idle timer."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
developer_instructions = """
__PROMPT__
"""
