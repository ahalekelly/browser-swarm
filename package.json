{
  "name": "browser-swarm",
  "version": "0.1.1",
  "description": "BrowserSwarm — one shared headless browser for every agent in a fan-out. npx installer.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ahalekelly/browser-swarm.git"
  },
  "os": ["darwin"],
  "cpu": ["arm64"],
  "bin": {
    "browser-swarm": "./install.sh"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:firefox": "node --test tests/firefox/*.test.js"
  },
  "files": ["install.sh"],
  "devDependencies": {
    "@playwright/mcp": "^0.0.78"
  }
}
