{
  "name": "browser-swarm",
  "version": "0.2.0",
  "description": "BrowserSwarm — one shared headless browser for every agent in a fan-out.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ahalekelly/browser-swarm.git"
  },
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "test": "node --test --test-timeout=60000 tests/*.test.js",
    "test:firefox": "node --test --test-timeout=120000 tests/firefox/*.test.js"
  },
  "devDependencies": {
    "@playwright/mcp": "^0.0.79"
  }
}
